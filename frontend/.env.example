# Environment Configuration Template
# Copy this to .env.local and fill in your specific values

# =============================================================================
# RATE LIMITING CONFIGURATION
# =============================================================================
# Configure rate limiting for different API endpoints
# Format: window (seconds), max (requests per window)

# API Rate Limits - General API endpoints
NEXT_PUBLIC_RATE_LIMIT_API_WINDOW=60
NEXT_PUBLIC_RATE_LIMIT_API_MAX=100

# Authentication Rate Limits (stricter for security)
NEXT_PUBLIC_RATE_LIMIT_AUTH_WINDOW=300
NEXT_PUBLIC_RATE_LIMIT_AUTH_MAX=5

# Server-Sent Events (SSE) Rate Limits
NEXT_PUBLIC_RATE_LIMIT_SSE_WINDOW=60
NEXT_PUBLIC_RATE_LIMIT_SSE_MAX=20

# File Upload Rate Limits
NEXT_PUBLIC_RATE_LIMIT_UPLOAD_WINDOW=300
NEXT_PUBLIC_RATE_LIMIT_UPLOAD_MAX=10

# Search Rate Limits
NEXT_PUBLIC_RATE_LIMIT_SEARCH_WINDOW=10
NEXT_PUBLIC_RATE_LIMIT_SEARCH_MAX=30

# Chat Rate Limits
NEXT_PUBLIC_RATE_LIMIT_CHAT_WINDOW=60
NEXT_PUBLIC_RATE_LIMIT_CHAT_MAX=60

# Advanced Rate Limiting Config (JSON format - optional, overrides individual settings above)
# NEXT_PUBLIC_RATE_LIMITS_CONFIG='{"api":{"window":60,"max":100},"auth":{"window":300,"max":5}}'

# =============================================================================
# SERVER-SENT EVENTS (SSE) CONFIGURATION
# =============================================================================
# SSE heartbeat interval in milliseconds (30000 = 30 seconds)
SSE_HEARTBEAT_INTERVAL=30000

# =============================================================================
# REDIS CONFIGURATION (Server-side only)
# =============================================================================
# Redis is used for distributed rate limiting in production
# In development, memory-based rate limiting is used by default

# Redis Server Host
REDIS_HOST=localhost

# Redis Server Port
REDIS_PORT=6379

# Redis Password (if authentication is enabled)
REDIS_PASSWORD=

# Redis Database Number (0-15)
REDIS_DB=0

# =============================================================================
# APPLICATION ENVIRONMENT
# =============================================================================
# Environment: development, staging, production
# Environment: development, staging, production
# Use 'development' for local development, 'production' for production deployments
NODE_ENV=development

# =============================================================================
# API CONFIGURATION
# =============================================================================
# API base URL for external services
NEXT_PUBLIC_API_URL=https://api.vana.ai

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# These are handled by the middleware and security configuration
# CSP nonces and security headers are auto-generated

# =============================================================================
# NEXT.JS CONFIGURATION
# =============================================================================
# Next.js specific environment variables can be added here
# Example:
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# DATABASE CONFIGURATION (if needed)
# =============================================================================
# Add your database connection strings here
# Example:
# DATABASE_URL=postgresql://username:password@localhost:5432/database

# =============================================================================
# EXTERNAL SERVICES (if needed)
# =============================================================================
# Add external service API keys and configuration here
# Example:
# OPENAI_API_KEY=sk-...
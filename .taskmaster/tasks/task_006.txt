# Task ID: 6
# Title: Install Missing Dependencies
# Status: pending
# Dependencies: 5
# Priority: medium
# Description: Add missing optional dependencies to pyproject.toml.
# Details:
Update `pyproject.toml` to include the missing dependencies with version constraints. Example:
```toml
[tool.poetry.dependencies]
PyPDF2 = "^3.0.0"
Pillow = "^9.0.0"
pytesseract = "^0.3.9"
tenacity = "^8.0.0"
```

# Test Strategy:
Run `poetry install` and verify that dependencies are installed without conflicts.

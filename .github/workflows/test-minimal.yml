name: Test Minimal

on:
  push:
    branches: [fix/ci-workflow-consolidation]
  workflow_dispatch:

jobs:
  test-basic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test basic functionality
        run: |
          echo "Testing basic workflow functionality"
          ls -la
          echo "Done"
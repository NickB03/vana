<artifact type="application/vnd.ant.react" title="Basic 3D Rotating Cube">
import { Canvas } from '@react-three/fiber';
import { OrbitControls } from '@react-three/drei';
import { useRef } from 'react';
import * as THREE from 'three';

function RotatingCube() {
  const meshRef = useRef<THREE.Mesh>(null);

  return (
    <mesh ref={meshRef} rotation={[1, 1, 0]}>
      <boxGeometry args={[2, 2, 2]} />
      <meshStandardMaterial
        color="#4f46e5"
        emissive="#4f46e5"
        emissiveIntensity={0.2}
        roughness={0.3}
        metalness={0.8}
      />
    </mesh>
  );
}

function Scene() {
  return (
    <>
      <ambientLight intensity={0.5} />
      <pointLight position={[10, 10, 10]} intensity={1} />
      <pointLight position={[-10, -10, -10]} intensity={0.5} color="#ff6b6b" />
      <RotatingCube />
      <OrbitControls enablePan={true} enableZoom={true} enableRotate={true} />
    </>
  );
}

export default function Basic3DScene() {
  return (
    <div style={{ width: '100%', height: '400px', background: 'linear-gradient(135deg, #1e1b4b 0%, #312e81 100%)', borderRadius: '8px' }}>
      <Canvas camera={{ position: [5, 5, 5], fov: 50 }}>
        <Scene />
      </Canvas>
      <div style={{
        position: 'absolute',
        bottom: '20px',
        left: '50%',
        transform: 'translateX(-50%)',
        color: 'white',
        textAlign: 'center',
        fontFamily: 'system-ui, sans-serif'
      }}>
        <p style={{ margin: 0, fontSize: '14px' }}>🎲 Basic 3D Scene</p>
        <p style={{ margin: 0, fontSize: '12px', opacity: 0.8 }}>Use mouse to rotate • Scroll to zoom</p>
      </div>
    </div>
  );
}
</artifact>
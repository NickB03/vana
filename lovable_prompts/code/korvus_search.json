{
  "title": "Search Korvus Memory",
  "target_file": "korvus_config/orvus_search.py",
  "description": "Create a function called `search_context(query: str, limit: 10)` that retrieves relevant memory from Supabase vector index.",
  "context": "Source: Korvus MEMORY API. Pgfvector is used via Supabase. This runs in Gkp Cloud Run.",
  "steps": [
    "Query the `vector_index` table with nearest vectors",
    "Compute cosine distance against embedded query",
    "Return list of summaries: response, source, datetime",
    "Sort by score descending"
  ]
}
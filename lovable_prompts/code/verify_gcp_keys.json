{
  "title": "GCP Config Validator",
  "target_file": "scripts/verify_gcp_keys.py",
  "description": "Create a terminal script that will validate Frontend keys, API access, and test associated Vertex AI services.",
  "context": "Used to validate service account configuration in GCP. This runs locally.",
  "steps": [
    "Create ` validate_keys() ` function",
    "Load `grc_account_credentials` from env",
    "Test API calls to `vertex.googleapis.com` for textembedding and text-chat",
    "Return status + explanation as JSON or error"
  ]
}
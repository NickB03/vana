fix: Critical security fixes and port consistency improvements

BREAKING CHANGE: CORS now only allows port 5173 (removed 5177, 5179)

Security fixes:
- Replace predictable timestamp-based session IDs with secure UUIDs
- Sanitize all error messages to prevent information leakage
- Remove hardcoded API key "secret-dev-vana-123"
- Fix CORS preflight handler to validate origins dynamically

Port consistency:
- Standardize on port 5173 for frontend (as configured in vite.config.ts)
- Update CORS to only allow the actual frontend port
- Create ALLOWED_ORIGINS constant for single source of truth
- Fix streaming response headers to use consistent origin

Documentation updates:
- Update incorrect date from "July 2024" to "July 10, 2025"
- Fix model version: Gemini 2.5 Pro → Gemini 2.0 Flash (actual)
- Clarify tool count: 8 core tools (plus optional memory)
- Ensure all documentation reflects actual system state

Code cleanup:
- Remove 5 deprecated web search implementations
- Clean up unused imports and dead code
- Improve error handling consistency

These changes address critical security vulnerabilities identified in code review
and ensure the system configuration is consistent across all components.

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>
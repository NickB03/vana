# API Key Configuration for Supabase Edge Functions
# This file is for reference only - actual keys are set via Supabase Secrets

# CORS Configuration (Production)
# Comma-separated list of allowed origins for CORS validation
# If not set, defaults to localhost development origins
# SECURITY: Never use wildcard (*) in production
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# OpenRouter API Keys
# Get API keys from: https://openrouter.ai/keys

# Chat, Titles, Summaries (Gemini 2.5 Flash Lite)
OPENROUTER_GEMINI_FLASH_KEY=sk-or-v1-...your_key_for_chat

# Image Generation (Gemini 2.5 Flash Image)
OPENROUTER_GEMINI_IMAGE_KEY=sk-or-v1-...your_key_for_images

# Deprecated: Z.ai API Key (Artifact Generation)
# Replaced by OPENROUTER_GEMINI_FLASH_KEY in migration to Gemini 3 Flash (January 2026)
# GLM_API_KEY=...your_glm_key

# Tavily API Key (Web Search)
# Get API key from: https://tavily.com/
# Used for real-time web search capabilities
TAVILY_API_KEY=tvly-...your_key

# Set these secrets in Supabase:
# supabase secrets set OPENROUTER_GEMINI_FLASH_KEY=... OPENROUTER_GEMINI_IMAGE_KEY=... TAVILY_API_KEY=...
# Deprecated: GLM_API_KEY (no longer required after Gemini migration)

# ============================================================================
# FRONTEND ENVIRONMENT VARIABLES (.env.local)
# ============================================================================
# These go in your frontend .env.local file (not Supabase secrets)

# Sentry Error Monitoring (Optional)
# Get your DSN from: https://sentry.io/settings/projects/YOUR_PROJECT/keys/
# Only active in production builds
VITE_SENTRY_DSN=https://your-dsn@sentry.io/project-id

# App Version for Sentry (Optional)
# Tags error reports with version for release tracking
# Typically set automatically in CI/CD pipelines
VITE_APP_VERSION=1.0.0

# Sentry Source Map Upload Configuration (Required for CI/CD)
# These environment variables are used by the Sentry Vite plugin to upload source maps
# during production builds. They should be set in your CI/CD environment (e.g., Cloudflare Pages)
# Get your auth token from: https://sentry.io/settings/account/api/auth-tokens/
# Required permissions: Project (Read & Write), Release (Admin), Organization (Read)
SENTRY_AUTH_TOKEN=your-auth-token-here

# Your Sentry organization slug (found in Sentry URL)
# Example: if your URL is https://sentry.io/organizations/my-org/, use "my-org"
SENTRY_ORG=your-org-slug

# Your Sentry project slug (found in Sentry URL)
# Example: if your URL is https://sentry.io/organizations/my-org/projects/my-project/, use "my-project"
SENTRY_PROJECT=your-project-slug

